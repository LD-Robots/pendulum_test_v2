<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>pendulum_pvt_control</name>
  <version>0.1.0</version>
  <description>
    PVT / MIT-mode (CiA-402 mode 5) impedance controller for the LDR pendulum
    testbed. On real hardware the controller streams target position, velocity,
    torque-feedforward and Kp/Kd every cycle (RxPDO 0x1601) and the myActuator X6
    runs the law tau = Kp*(q_d - q) + Kd*(qd_d - qd) + tau_ff internally at 1 kHz.
    In Gazebo sim the same controller replicates that law in software and writes
    the effort command interface. Host-side gravity / inertia / viscous
    feedforward is added through the torque-feedforward channel.
  </description>
  <maintainer email="andrei.dragomir@ldrobots.ro">Andrei Dragomir</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>controller_interface</depend>
  <depend>hardware_interface</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>
  <depend>realtime_tools</depend>
  <depend>std_srvs</depend>
  <depend>trajectory_msgs</depend>

  <exec_depend>joint_state_broadcaster</exec_depend>
  <exec_depend>controller_manager</exec_depend>
  <exec_depend>robot_state_publisher</exec_depend>
  <exec_depend>xacro</exec_depend>
  <exec_depend>ethercat_driver</exec_depend>
  <exec_depend>gz_ros2_control</exec_depend>
  <exec_depend>pendulum_description</exec_depend>
  <exec_depend>pendulum_gazebo</exec_depend>
  <exec_depend>pendulum_pd_control</exec_depend>
  <exec_depend>rclpy</exec_depend>
  <exec_depend>sensor_msgs</exec_depend>

  <!-- pvt_sim_gui.py: offline visualiser, no ROS deps -->
  <exec_depend>python3-numpy</exec_depend>
  <exec_depend>python3-matplotlib</exec_depend>
  <exec_depend>python3-tk</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <controller_interface plugin="${prefix}/pendulum_pvt_controller_plugin.xml"/>
  </export>
</package>
